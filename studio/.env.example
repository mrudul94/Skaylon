# Copy to .env (git-ignored). The Studio reads SANITY_STUDIO_* at build time.
SANITY_STUDIO_PROJECT_ID=
SANITY_STUDIO_DATASET=production

# Only for `npm run seed` (Editor token from sanity.io/manage → API → Tokens).
SANITY_WRITE_TOKEN=
