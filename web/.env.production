# Non-secret, build-time public values (inlined by Next). Secrets never go here:
# use `wrangler secret put` for production and .dev.vars / .env.local locally.
NEXT_PUBLIC_SITE_URL=https://skaylon.com
NEXT_PUBLIC_SANITY_PROJECT_ID=94bjvovm
NEXT_PUBLIC_SANITY_DATASET=production
